import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttonWrapper: {
    width: "100%",
  },
  button: {
    fontWeight: "bold",
    textAlign: "center",
    paddingVertical: 16,
    backgroundColor: "red",
    color: "white",
  },
});

export default styles;
