import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttonWrapper: {
    marginTop: 16,
    width: "100%",
  },
  button: {
    fontWeight: "bold",
    width: "100%",
    textAlign: "center",
    paddingVertical: 16,
    backgroundColor: "red",
    color: "white",
  },
});

export default styles;
